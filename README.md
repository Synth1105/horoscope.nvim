# horoscope

A Lush theme for Neovim, with Shipwright export to `colors/horoscope.lua`.

Requirements:

- `lush.nvim`
- `shipwright.nvim` or `shipwright`

Build:

```sh
shipwright shipwright_build.lua
```
